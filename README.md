# Todo App

This is a simple todo application to highlight basic concepts learnt with regard to the *MERN* stack.

The `/backend` component that is implemented using [KeystoneJS](https://www.keystonejs.com/) and it
exposes a `GraphQL` interface for consumption by the `/frontend`.

The `/frontend` is implemented using *NextJS*.