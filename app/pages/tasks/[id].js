import {useRouter} from "next/router";

const Task = () => {
    const router = useRouter();

    const { query } = router;

    // find task by id


    return <h1>Task </h1>
};

export default Task;